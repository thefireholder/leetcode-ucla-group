output = [1]
        
        for i in range(1, len(nums)):
            output.append(output[-1]*nums[i-1])
            
        post = 1
        for j in range(len(nums) - 1, -1, -1) :
            output[j] *= post
            post *= nums[j]
            
        return output
